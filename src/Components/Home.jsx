import React from 'react';
import Slider from './Slider';

const Home = () => {
    return (
        <div>
            <section className="slider-section">
                <Slider></Slider>
            </section>
        </div>
    );
};

export default Home;